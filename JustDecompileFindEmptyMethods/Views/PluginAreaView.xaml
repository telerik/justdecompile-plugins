<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FindEmptyMethods.Views"
             x:Class="FindEmptyMethods.Views.PluginAreaView"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <d:UserControl.DataContext>
        <local:PluginAreaView/>
    </d:UserControl.DataContext>
    <Grid Margin="2">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ScrollViewer VerticalAlignment="Center" Grid.Row="1" Margin="2" VerticalScrollBarVisibility="Auto">
            <TextBlock TextWrapping="Wrap" Text="{Binding Text}"/>
        </ScrollViewer>
        <Button Content="close" HorizontalAlignment="Center" VerticalAlignment="Center" IsCancel="True" Command="{Binding CloseCommand}"/>
    </Grid>
</UserControl>
